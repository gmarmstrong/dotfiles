# Verbosity
verbose=blank,footnote,label,new-id,affected,edit,special,project,sync,unwait

# Files
data.location=~/.local/share/task

# Inthe.AM
taskd.certificate=~/.intheam/private.certificate.pem
taskd.key=~/.intheam/private.key.pem
taskd.ca=~/.intheam/ca.cert.pem
taskd.server=taskwarrior.inthe.am:53589
taskd.credentials=inthe_am/guthrie.armstrong/9cc08d9a-d660-471d-9556-6980a6a1162d
taskd.trust=ignore hostname

# Color theme
include /usr/share/taskwarrior/dark-gray-256.theme

# Default command
report.custom.columns=id,project,tags,recur,due.relative,description
report.custom.description=Things to do right now
report.custom.filter=status:pending and +READY
report.custom.labels=ID,Project,Tags,Recur,Due,Description
report.custom.sort=project+,due+
default.command=custom
