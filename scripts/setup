#!/bin/bash

echo "This script isn't done yet!"
exit 0

. "$HOME/dotfiles/scripts/functions.sh"

# TODO Always link .iterm2_definition if $SSH_CLIENT is set (before setting TERM)
    # i.e., before sourcing zshrc

test_internet_connection
check_user_privileges
